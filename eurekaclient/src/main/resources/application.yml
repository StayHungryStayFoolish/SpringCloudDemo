eureka:
  client:
    serviceUrl:
     defaultZone: http://localhost:8761/eureka/

server:
  port: 8762
spring:
  application:
    name: eureka-client



#    spring.application.name 服务与服务之间相互调用一般根据这个 name